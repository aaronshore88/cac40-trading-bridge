services:
  - type: web
    name: cac40-trading-bridge
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn tv_mt5_bridge_v05:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
